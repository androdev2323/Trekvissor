<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.win7.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".page1"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".statestolocation.statestopage" />
        <activity android:name=".page10.page10">

        </activity>
        <activity android:name=".page6">



        </activity>
        <activity android:name=".weather" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/api" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".map" />
        <activity android:name=".locationmenu" />
        <activity android:name=".signin" />
        <activity android:name=".MainActivity" />
        <activity android:name=".emergency" />
        <activity android:name=".mainmenu">



        </activity>
    </application>

</manifest>